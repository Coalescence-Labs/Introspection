# Optional: Supabase (server-only, read-only). When set, questions and today config
# are loaded from Supabase; when unset, from local content/*.ts.
NEXT_PUBLIC_SUPABASE_URL=<SUBSTITUTE_SUPABASE_URL>
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<SUBSTITUTE_SUPABASE_PUBLISHABLE_KEY>