import { WelcomePageShell } from "@/components/WelcomePageShell";

/** Welcome (landing) route. Renders full-screen sections with particle background and section scroll. */
export default function WelcomePage() {
  return <WelcomePageShell />;
}
